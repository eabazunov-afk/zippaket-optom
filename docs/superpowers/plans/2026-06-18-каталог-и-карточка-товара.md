# Каталог и карточка товара — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Сделать рабочую страницу товара и связать её с уже существующим каталогом, учитывая упаковку (мин. кол-во/кратность), на реальных данных из таблицы `products`.

**Architecture:** Расширяем текущий PHP-сайт. Каталог (`katalog_zip_paketov/katalog.php`) и класс `Catalog` (`includes/catalog_functions.php`) уже есть. Добавляем недостающий метод `getProductById()`, чистые функции форматирования карточки (юнит-тестируемые), страницу `product.php`, маршрут `/product/<id>` и отображение полей упаковки. Визуальный premium-редизайн — отдельный План 6; здесь сохраняем текущий стиль, добавляем функциональность.

**Tech Stack:** PHP 8.3 (CLI: `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe`), PDO/MySQL 8.4 (Laragon), PHPUnit 9.6, Apache mod_rewrite (`.htaccess`).

## Global Constraints

- Стек — PHP «на месте», процедурный стиль + PDO; следовать паттернам существующего кода.
- Запросы к БД — подготовленные выражения.
- Бизнес-логику количеств брать из Плана 1: `normalize_quantity()`, `is_valid_quantity()` (`includes/cart_quantity.php`).
- Поля упаковки из миграции Плана 1: `products.min_order_qty`, `products.qty_step`, `products.pack_label`.
- Корзина и оформление — это План 3. Здесь кнопка «В корзину» только готовит данные (data-атрибуты), реальное добавление — позже.
- Тесты: чистые функции — PHPUnit (как в Плане 1). Запуск phpunit: `"<php>" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests` из `www/` (composer-скрипт `test` не используется — на Windows его shim требует php в PATH).
- Локальная БД/MySQL не в PATH — полные пути (см. память проекта). Дамп грузить с `--force` (генерируемые столбцы в strict mode).
- Работаем в feature-ветке; коммит после каждого пройденного гейта.
- `<php>` далее = `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe`; `<mysql>` = `C:\laragon\bin\mysql\mysql-8.4.3-winx64\bin\mysql.exe`.

---

## File Structure

- `www/includes/product_view.php` — чистые функции форматирования товара (цена, размер, наличие, кол-во, подпись упаковки). Без БД. Юнит-тестируемо.
- `www/tests/ProductViewTest.php` — тесты для product_view.php.
- `www/includes/catalog_functions.php` — добавить метод `getProductById(int $id)`.
- `www/api/product.php` — отрефакторить quick-view на `getProductById()` (убрать дублированный PDO).
- `www/product.php` — страница товара (новая).
- `www/.htaccess` — маршрут `/product/<id>` → `product.php?id=`.
- `www/katalog_zip_paketov/katalog.php` — показать подпись упаковки на карточках (минимальная правка).
- (Task 1, без коммита кода) локальная dev-БД с данными товаров для прогона страниц.

---

## Task 1: Локальная dev-БД с данными товаров

**Files:**
- Modify (локально, НЕ коммитится): `www/includes/config.php` — креды локальной БД.

**Interfaces:**
- Consumes: дамп `c103264_zippaket_optom_ru.sql`, миграция `db/migrations/2026-06-18-orders-schema.sql`.
- Produces: рабочая локальная БД `c103264_zippaket_optom_ru` с таблицами и товарами; сайт подключается локально.

- [ ] **Step 1: Создать feature-ветку**

```bash
cd "C:/Users/USER/Documents/Сайт"
git checkout master && git pull
git checkout -b feature/catalog-product
```

- [ ] **Step 2: Создать локальную БД и пользователя**

Run (значения паролей — для локалки, не секрет):
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 --execute="CREATE DATABASE IF NOT EXISTS c103264_zippaket_optom_ru CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```
Expected: без ошибок.

- [ ] **Step 3: Загрузить дамп (с --force из-за генерируемых столбцов)**

```bash
cd "C:/Users/USER/Documents/Сайт"
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 --force c103264_zippaket_optom_ru < c103264_zippaket_optom_ru.sql
```
Expected: возможны ошибки по генерируемым столбцам (`price_rub`, `utm_source`) — это ок, таблицы создаются. Проверка ниже.

- [ ] **Step 4: Применить миграцию Плана 1**

```bash
cd "C:/Users/USER/Documents/Сайт"
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru < db/migrations/2026-06-18-orders-schema.sql
```
Expected: без ошибок (поля упаковки + таблицы orders/order_items добавлены).

- [ ] **Step 5: Заполнить товары (если INSERT'ы упали) и задать значения упаковки**

Проверить кол-во товаров:
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="SELECT COUNT(*) FROM products;"
```
Если 0 (INSERT'ы упали из-за `price_rub`), пересоздать данные товаров из дампа без генерируемого столбца:
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="SET sql_mode='';"
```
Затем повторить Step 3 c `SET SESSION sql_mode='';` в начале:
```bash
cd "C:/Users/USER/Documents/Сайт"
( echo "SET SESSION sql_mode='';" && cat c103264_zippaket_optom_ru.sql ) | "C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 --force c103264_zippaket_optom_ru
```
Задать демонстрационные значения упаковки:
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="UPDATE products SET min_order_qty=100, qty_step=50, pack_label='уп. 50 шт' WHERE min_order_qty IS NULL OR min_order_qty<=1;"
```
Expected: `COUNT(*)` > 0 после загрузки.

- [ ] **Step 6: Прописать локальные креды в config.php (не коммитится)**

В `www/includes/config.php` временно для локальной разработки убедиться, что:
```php
define('DB_HOST', 'localhost');
define('DB_PORT', '3306');
define('DB_NAME', 'c103264_zippaket_optom_ru');
define('DB_USER', 'root');
define('DB_PASS', '');
```
(Если на проде другие — вернуть перед деплоем; файл в .gitignore.)

> ПРИМЕЧАНИЕ: если MySQL не запущен — инициализировать и поднять Laragon MySQL (см. память проекта `php-mysql-env`).

- [ ] **Step 7: Проверить подключение из PHP**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "require 'includes/config.php'; \$db=getDbConnection(); echo 'products: '.\$db->query('SELECT COUNT(*) FROM products')->fetchColumn();"
```
Expected: `products: <N>` где N > 0.

- [ ] **Step 8: Чекпоинт (без кода — фиксируем только намерение)**

Кода для коммита нет (config.php игнорируется). Зафиксировать в логе словами: «локальная БД готова». Переходить к Task 2.

---

## Task 2: Чистые функции форматирования товара

**Files:**
- Create: `www/includes/product_view.php`
- Test: `www/tests/ProductViewTest.php`

**Interfaces:**
- Consumes: `normalize_quantity()` из `includes/cart_quantity.php`.
- Produces:
  - `pv_format_price(float $price): string` — "94,66 ₽" (2 знака, запятая, пробел-разделитель тысяч).
  - `pv_format_size(?int $width, ?int $height): string` — "200×150 мм"; пустая строка, если оба null.
  - `pv_stock_status(int $stock): array` — `['in_stock'=>bool, 'label'=>string, 'count_label'=>string]`; при stock>0: in_stock=true, label='В наличии', count_label='12 100 шт'; при 0: in_stock=false, label='Под заказ', count_label=''.
  - `pv_default_qty(int $min, int $step): int` — стартовое количество для карточки = `normalize_quantity($min, $min, $step)`.
  - `pv_pack_note(int $min, int $step): string` — "мин. 100 · кратно 50"; если step<=1: "мин. 100".

- [ ] **Step 1: Написать падающий тест**

`www/tests/ProductViewTest.php`:

```php
<?php
use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../includes/cart_quantity.php';
require_once __DIR__ . '/../includes/product_view.php';

class ProductViewTest extends TestCase
{
    public function testFormatPrice(): void
    {
        $this->assertSame('94,66 ₽', pv_format_price(94.66));
        $this->assertSame('1 250,00 ₽', pv_format_price(1250));
    }

    public function testFormatSize(): void
    {
        $this->assertSame('200×150 мм', pv_format_size(200, 150));
        $this->assertSame('', pv_format_size(null, null));
    }

    public function testStockStatusInStock(): void
    {
        $s = pv_stock_status(12100);
        $this->assertTrue($s['in_stock']);
        $this->assertSame('В наличии', $s['label']);
        $this->assertSame('12 100 шт', $s['count_label']);
    }

    public function testStockStatusOutOfStock(): void
    {
        $s = pv_stock_status(0);
        $this->assertFalse($s['in_stock']);
        $this->assertSame('Под заказ', $s['label']);
        $this->assertSame('', $s['count_label']);
    }

    public function testDefaultQty(): void
    {
        $this->assertSame(100, pv_default_qty(100, 50));
        $this->assertSame(1, pv_default_qty(1, 1));
    }

    public function testPackNote(): void
    {
        $this->assertSame('мин. 100 · кратно 50', pv_pack_note(100, 50));
        $this->assertSame('мин. 100', pv_pack_note(100, 1));
    }
}
```

- [ ] **Step 2: Запустить — убедиться, что падает**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests/ProductViewTest.php
```
Expected: FAIL — `Failed opening required '.../product_view.php'`.

- [ ] **Step 3: Реализовать функции**

`www/includes/product_view.php`:

```php
<?php
// Чистые функции форматирования товара для каталога/карточки. Без БД.
require_once __DIR__ . '/cart_quantity.php';

function pv_format_price(float $price): string
{
    return number_format($price, 2, ',', ' ') . ' ₽';
}

function pv_format_size(?int $width, ?int $height): string
{
    if ($width === null && $height === null) {
        return '';
    }
    return ($width ?? 0) . '×' . ($height ?? 0) . ' мм';
}

function pv_stock_status(int $stock): array
{
    if ($stock > 0) {
        return [
            'in_stock'    => true,
            'label'       => 'В наличии',
            'count_label' => number_format($stock, 0, ',', ' ') . ' шт',
        ];
    }
    return ['in_stock' => false, 'label' => 'Под заказ', 'count_label' => ''];
}

function pv_default_qty(int $min, int $step): int
{
    return normalize_quantity($min, $min, $step);
}

function pv_pack_note(int $min, int $step): string
{
    $note = 'мин. ' . $min;
    if ($step > 1) {
        $note .= ' · кратно ' . $step;
    }
    return $note;
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS (все тесты Плана 1 + 6 новых в ProductViewTest).

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/product_view.php www/tests/ProductViewTest.php
git commit -m "feat: чистые функции форматирования товара (цена/размер/наличие/упаковка) с тестами"
```

---

## Task 3: Метод getProductById + рефактор api/product.php

**Files:**
- Modify: `www/includes/catalog_functions.php` (добавить метод в класс `Catalog`)
- Modify: `www/api/product.php` (использовать `getProductById`, убрать дублированный PDO)

**Interfaces:**
- Consumes: класс `Catalog` (есть), `getDbConnection()` из config.php.
- Produces: метод `Catalog::getProductById(int $id): ?array` — возвращает строку товара (только `is_active = 1`) с теми же доп. полями форматирования, что и `getProducts()` (`formatted_price`, `formatted_stock`, `size_display`, `image_url`, `type_icon`, `type_name`), либо `null`, если не найден.

- [ ] **Step 1: Добавить метод getProductById в класс Catalog**

В `www/includes/catalog_functions.php` внутри класса `Catalog` (например, после `getProducts()`), добавить:

```php
    /**
     * Получить один товар по id (только активные). null, если не найден.
     */
    public function getProductById(int $id): ?array
    {
        try {
            $stmt = $this->db->prepare("SELECT * FROM products WHERE id = :id AND is_active = 1");
            $stmt->bindValue(':id', $id, PDO::PARAM_INT);
            $stmt->execute();
            $product = $stmt->fetch();
            if (!$product) {
                return null;
            }

            $product['formatted_price'] = number_format($product['price_rub'], 2, ',', ' ') . ' ₽';
            $product['formatted_stock'] = number_format($product['stock_quantity'], 0, ',', ' ');
            $product['size_display'] = $product['width'] . '×' . $product['height'] . ' мм';
            $product['image_url'] = !empty($product['image_url']) ? $product['image_url'] : '/images/no-image.jpg';

            if (strpos($product['category'], 'слайдер') !== false) {
                $product['type_icon'] = 'sliders-h';
                $product['type_name'] = 'Слайдер';
            } else {
                $product['type_icon'] = 'lock';
                $product['type_name'] = 'ZIP-LOCK';
            }

            return $product;
        } catch (PDOException $e) {
            error_log("Ошибка получения товара по id: " . $e->getMessage());
            return null;
        }
    }
```

- [ ] **Step 2: Проверить синтаксис**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l includes/catalog_functions.php
```
Expected: `No syntax errors detected`.

- [ ] **Step 3: Проверить метод вживую на локальной БД**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "require 'includes/catalog_functions.php'; \$c=new Catalog(); \$p=\$c->getProductById(1); echo \$p ? (\$p['full_name'].' | '.\$p['formatted_price']) : 'NULL'; echo PHP_EOL; var_dump(\$c->getProductById(999999));"
```
Expected: для id=1 — название + цена; для id=999999 — `NULL`.

- [ ] **Step 4: Отрефакторить api/product.php на getProductById**

Заменить в `www/api/product.php` блок получения товара (строки с собственным `new PDO(...)` и запросом) на использование метода. Итоговое начало файла:

```php
<?php
require_once __DIR__ . '/../includes/config.php';
require_once __DIR__ . '/../includes/catalog_functions.php';

$id = isset($_GET['id']) ? (int)$_GET['id'] : 0;
$quick = isset($_GET['quick']);

if (!$id) {
    http_response_code(404);
    die('Товар не найден');
}

$catalog = new Catalog();
$product = $catalog->getProductById($id);

if (!$product) {
    http_response_code(404);
    die('Товар не найден');
}

if ($quick) {
    $price = number_format($product['price_rub'], 2, ',', ' ');
    $size = $product['width'] . '×' . $product['height'] . ' мм';
    ?>
```

Остальную часть quick-view разметки (от `<div class="quick-view-product">` до закрытия) оставить как есть. Удалить старый `try { $db = new PDO(... ) ... }` и финальный `catch`, заменив на код выше (логика та же, источник данных — метод).

- [ ] **Step 5: Проверить синтаксис и quick-view вживую**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l api/product.php
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['id'=>1,'quick'=>1]; require 'api/product.php';" | head -5
```
Expected: нет синтаксических ошибок; вывод содержит HTML quick-view (тег `quick-view-product`).

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/catalog_functions.php www/api/product.php
git commit -m "feat: Catalog::getProductById + рефактор api/product.php (DRY, без дублированного PDO)"
```

---

## Task 4: Страница товара product.php + маршрут /product/<id>

**Files:**
- Create: `www/product.php`
- Modify: `www/.htaccess` (маршрут)

**Interfaces:**
- Consumes: `Catalog::getProductById()`, функции `product_view.php`, `header.php`/`footer.php`.
- Produces: страница товара по `/product.php?id=` и ЧПУ `/product/<id>`; 404 при отсутствии.

- [ ] **Step 1: Создать product.php**

`www/product.php`:

```php
<?php
require_once __DIR__ . '/includes/config.php';
require_once __DIR__ . '/includes/catalog_functions.php';
require_once __DIR__ . '/includes/product_view.php';

$id = isset($_GET['id']) ? (int)$_GET['id'] : 0;
$catalog = new Catalog();
$product = $id ? $catalog->getProductById($id) : null;

if (!$product) {
    http_response_code(404);
    $pageTitle = 'Товар не найден';
} else {
    $pageTitle = $product['meta_title'] ?: ($product['full_name'] . ' — купить оптом | ZLOCK');
}

$minQty  = (int)($product['min_order_qty'] ?? 1);
$qtyStep = (int)($product['qty_step'] ?? 1);
?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <?php if ($product): ?>
    <meta name="description" content="<?= htmlspecialchars($product['meta_description'] ?: $product['full_name']) ?>">
    <link rel="canonical" href="https://zippaket-optom.ru/product/<?= (int)$product['id'] ?>">
    <?php endif; ?>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/catalog.css">
    <?php if ($product):
        $stock = pv_stock_status((int)$product['stock_quantity']);
    ?>
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Product",
      "name": <?= json_encode($product['full_name'], JSON_UNESCAPED_UNICODE) ?>,
      "image": <?= json_encode('https://zippaket-optom.ru' . $product['image_url'], JSON_UNESCAPED_UNICODE) ?>,
      "description": <?= json_encode($product['meta_description'] ?: $product['full_name'], JSON_UNESCAPED_UNICODE) ?>,
      "offers": {
        "@type": "Offer",
        "price": "<?= number_format((float)$product['price_rub'], 2, '.', '') ?>",
        "priceCurrency": "RUB",
        "availability": "<?= $stock['in_stock'] ? 'https://schema.org/InStock' : 'https://schema.org/PreOrder' ?>"
      }
    }
    </script>
    <?php endif; ?>
</head>
<body>
    <div class="site-wrapper">
        <?php include __DIR__ . '/header.php'; ?>
        <main class="main-content">
        <?php if (!$product): ?>
            <section class="catalog-header-section"><div class="container">
                <div class="no-products">
                    <i class="fas fa-box-open"></i>
                    <h1>Товар не найден</h1>
                    <p>Возможно, он снят с продажи.</p>
                    <a href="/katalog_zip_paketov" class="btn btn-primary">В каталог</a>
                </div>
            </div></section>
        <?php else:
            $size = pv_format_size($product['width'] !== null ? (int)$product['width'] : null, $product['height'] !== null ? (int)$product['height'] : null);
        ?>
            <div class="breadcrumbs-section"><div class="container">
                <div class="breadcrumbs">
                    <a href="/">Главная</a>
                    <span class="separator"><i class="fas fa-chevron-right"></i></span>
                    <a href="/katalog_zip_paketov">Каталог</a>
                    <span class="separator"><i class="fas fa-chevron-right"></i></span>
                    <span class="current"><?= htmlspecialchars($product['short_name'] ?: $product['full_name']) ?></span>
                </div>
            </div></div>

            <section class="catalog-section"><div class="container">
                <div class="product-page" style="display:flex;gap:30px;flex-wrap:wrap;align-items:flex-start">
                    <div class="product-page-image" style="flex:1 1 320px;max-width:420px">
                        <img src="<?= htmlspecialchars($product['image_url']) ?>" alt="<?= htmlspecialchars($product['full_name']) ?>" style="width:100%;border-radius:16px;background:#f8fafc;padding:20px">
                    </div>
                    <div class="product-page-info" style="flex:2 1 360px">
                        <h1 style="margin-top:0"><?= htmlspecialchars($product['full_name']) ?></h1>
                        <div class="product-specs" style="margin:14px 0">
                            <?php if ($size): ?><span class="spec-item"><i class="fas fa-ruler"></i> <?= $size ?></span><?php endif; ?>
                            <?php if (!empty($product['thickness'])): ?><span class="spec-item"><i class="fas fa-layer-group"></i> <?= (int)$product['thickness'] ?> мкм</span><?php endif; ?>
                            <?php if (!empty($product['color'])): ?><span class="spec-item"><i class="fas fa-palette"></i> <?= htmlspecialchars($product['color']) ?></span><?php endif; ?>
                        </div>
                        <div class="product-pricing" style="margin:14px 0">
                            <span class="current-price" style="font-size:1.8rem;font-weight:800"><?= pv_format_price((float)$product['price_rub']) ?></span>
                            <span class="price-unit">/ шт</span>
                        </div>
                        <div class="stock-info <?= $stock['in_stock'] ? 'in-stock' : 'out-of-stock' ?>" style="margin:10px 0">
                            <i class="fas <?= $stock['in_stock'] ? 'fa-check-circle' : 'fa-clock' ?>"></i>
                            <?= $stock['label'] ?><?= $stock['count_label'] ? ': ' . $stock['count_label'] : '' ?>
                        </div>
                        <div class="pack-note" style="color:#64748b;font-size:0.9rem;margin-bottom:14px">
                            <?= htmlspecialchars(pv_pack_note($minQty, $qtyStep)) ?><?= !empty($product['pack_label']) ? ' (' . htmlspecialchars($product['pack_label']) . ')' : '' ?>
                        </div>
                        <div class="product-actions" style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
                            <input type="number" id="qty" class="filter-input small" value="<?= pv_default_qty($minQty, $qtyStep) ?>" min="<?= $minQty ?>" step="<?= $qtyStep ?>" style="width:110px">
                            <button class="btn btn-primary add-to-cart"
                                    data-id="<?= (int)$product['id'] ?>"
                                    data-name="<?= htmlspecialchars($product['full_name']) ?>"
                                    data-price="<?= $product['price_rub'] ?>"
                                    data-min="<?= $minQty ?>"
                                    data-step="<?= $qtyStep ?>">
                                <i class="fas fa-shopping-cart"></i> В корзину
                            </button>
                            <a href="/index.php#calculator" class="btn btn-outline">
                                <i class="fas fa-calculator"></i> С логотипом? Рассчитать
                            </a>
                        </div>
                    </div>
                </div>
            </div></section>
        <?php endif; ?>
        </main>
        <?php include __DIR__ . '/footer.php'; ?>
    </div>
    <script src="/js/script.js"></script>
</body>
</html>
```

- [ ] **Step 2: Проверить синтаксис**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l product.php
```
Expected: `No syntax errors detected`.

- [ ] **Step 3: Проверить рендер вживую (существующий товар)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['id'=>1]; require 'product.php';" | grep -E "Product|В корзину|application/ld" | head -5
```
Expected: вывод содержит JSON-LD `"@type": "Product"` и кнопку «В корзину».

- [ ] **Step 4: Проверить 404 для несуществующего товара**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['id'=>999999]; require 'product.php';" | grep -E "Товар не найден" | head -2
```
Expected: содержит «Товар не найден».

- [ ] **Step 5: Добавить маршрут в .htaccess**

В `www/.htaccess` после правила для `katalog_zip_paketov` (перед общим блоком «Сжатие текстовых файлов») добавить:

```apache
# ЧПУ для страницы товара: /product/123 -> product.php?id=123
RewriteRule ^product/([0-9]+)/?$ product.php?id=$1 [L,QSA]
```

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/product.php www/.htaccess
git commit -m "feat: страница товара product.php + ЧПУ /product/<id> + JSON-LD Product"
```

---

## Task 5: Подпись упаковки на карточках каталога

**Files:**
- Modify: `www/katalog_zip_paketov/katalog.php` (блок карточки товара, секция «Все товары»)

**Interfaces:**
- Consumes: поля `min_order_qty`, `qty_step` (есть в строке `products` после миграции), `pv_pack_note()`.
- Produces: на карточке товара выводится подпись упаковки.

- [ ] **Step 1: Подключить product_view в katalog.php**

В начале `www/katalog_zip_paketov/katalog.php` после строки подключения `catalog_functions.php` добавить:

```php
require_once __DIR__ . '/../includes/product_view.php';
```

- [ ] **Step 2: Вывести подпись упаковки в карточке**

В секции «Сетка товаров» внутри `.product-info`, сразу после блока `<div class="product-stock"> ... </div>` (перед `<div class="product-pricing">`), добавить:

```php
                                    <div class="pack-note" style="color:#94a3b8;font-size:0.8rem;margin:4px 0">
                                        <?= htmlspecialchars(pv_pack_note((int)($product['min_order_qty'] ?? 1), (int)($product['qty_step'] ?? 1))) ?>
                                    </div>
```

- [ ] **Step 3: Проверить синтаксис и рендер каталога**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l katalog_zip_paketov/katalog.php
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "chdir('katalog_zip_paketov'); \$_GET=[]; require 'katalog.php';" | grep -E "мин\. " | head -3
```
Expected: нет синтаксических ошибок; вывод содержит подпись «мин. …».

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/katalog_zip_paketov/katalog.php
git commit -m "feat: подпись упаковки (мин/кратность) на карточках каталога"
```

---

## Task 6: Финальные гейты Плана 2

**Files:** —

**Interfaces:**
- Consumes: всё из Tasks 1–5.
- Produces: подтверждение готовности каталога + страницы товара.

- [ ] **Step 1: Прогнать все тесты**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS — тесты Плана 1 (9) + ProductView (6) зелёные.

- [ ] **Step 2: Линт всех затронутых PHP**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
PHP="C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe"
"$PHP" -l includes/product_view.php && "$PHP" -l includes/catalog_functions.php && "$PHP" -l api/product.php && "$PHP" -l product.php && "$PHP" -l katalog_zip_paketov/katalog.php
```
Expected: `No syntax errors detected` для каждого.

- [ ] **Step 3: Сквозная проверка вживую (каталог → товар)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
PHP="C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe"
"$PHP" -r "\$_GET=['id'=>1]; require 'product.php';" | grep -c "В корзину"
```
Expected: `1` (кнопка присутствует).

- [ ] **Step 4: Чистота рабочего дерева**

```bash
cd "C:/Users/USER/Documents/Сайт"
git status -s
```
Expected: пусто (всё закоммичено; локальный config.php игнорируется).

---

## Self-Review (выполнено при написании плана)

- **Покрытие spec:** каталог на `products` — уже есть, расширен подписью упаковки (Task 5); карточка товара (страница) — Task 4; остаток/наличие — `pv_stock_status` (Task 2) + вывод; мин/кратность — поля из Плана 1, `pv_default_qty`/`pv_pack_note` (Task 2), счётчик на странице (Task 4); связка с калькулятором — кнопка на странице (Task 4); ЧПУ + meta/JSON-LD — Task 4; `getProductById` (отмеченный TODO в коде) — Task 3. Корзина/checkout — План 3 (вне объёма). Premium-визуал — План 6 (вне объёма).
- **Плейсхолдеры:** примечания (локальные креды, «если INSERT'ы упали») — операционные инструкции с конкретными командами, не пропуски. Код-шаги содержат полный код.
- **Согласованность типов:** `pv_format_price/pv_format_size/pv_stock_status/pv_default_qty/pv_pack_note` определены в Task 2 и используются с теми же сигнатурами в Tasks 4–5; `getProductById(int): ?array` определён в Task 3 и вызывается в Tasks 3–4; поля `min_order_qty`/`qty_step`/`pack_label` соответствуют миграции Плана 1; `normalize_quantity` — из Плана 1.
