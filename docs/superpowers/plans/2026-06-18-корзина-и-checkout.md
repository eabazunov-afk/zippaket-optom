# Корзина и checkout — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Серверная сессионная корзина, страница корзины, оформление заказа (физ/юр, доставка, способ оплаты) и создание заказа в БД с серверным пересчётом сумм и snapshot цен. Без онлайн-оплаты (она — План 4) и без уведомлений в CRM/Telegram (План 5).

**Architecture:** Расширяем текущий PHP-сайт. Корзина хранится в `$_SESSION['cart']` как `[product_id => qty]`; цены/наличие всегда берутся из БД (клиенту не доверяем). Бизнес-логика (построение строки, суммы, валидация checkout, номер заказа) — в чистых функциях с тестами. Создание заказа — в транзакции, с записью в `orders`/`order_items` (snapshot названия и цены). После оформления — переход на страницу успеха со статусом «ожидает оплаты» (реальная оплата подключается в Плане 4).

**Tech Stack:** PHP 8.3 (CLI: `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe`), PDO/MySQL 8.4, PHPUnit 9.6, сессии PHP, reCAPTCHA v3 (ключ в config), CSRF (функции generateCsrfToken/verifyCsrfToken уже есть в config.php).

## Global Constraints

- Стек — PHP «на месте», процедурный стиль + PDO; следовать паттернам кода.
- Цены, наличие, суммы и валидация количеств — ТОЛЬКО на сервере (клиенту не доверяем). Количества — через `normalize_quantity()`/`is_valid_quantity()` (План 1, `includes/cart_quantity.php`).
- Snapshot: в `order_items` пишутся `name_snapshot`, `price_snapshot` на момент заказа.
- Поля заказа и статусы — по миграции Плана 1 (`orders`, `order_items`); стартовый статус заказа: `pending_payment` (оплата в Плане 4) либо `new`→`pending_payment`.
- Способ оплаты `invoice` (по счёту) доступен ТОЛЬКО для `customer_type=company`.
- Для `company` обязательны `company_name` и `inn`; `kpp`/`legal_address` опциональны.
- CSRF на формах/эндпоинтах изменения корзины и checkout; reCAPTCHA v3 на checkout.
- Все выводимые данные экранируются (htmlspecialchars / json_encode).
- Тесты чистых функций — PHPUnit. Запуск из `www/`: `"<php>" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests`.
- `<php>` = `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe`. Локальная БД готова (49 товаров).
- Работаем в feature-ветке `feature/cart-checkout`; коммит после каждого гейта.

---

## File Structure

- `www/includes/cart_logic.php` — ЧИСТЫЕ функции корзины/заказа (без сессии/БД): построение строки, суммы, номер заказа. Тестируемо.
- `www/includes/checkout_validation.php` — ЧИСТЫЕ функции валидации checkout (поля по типу клиента, ИНН, способ оплаты, доставка). Тестируемо.
- `www/includes/cart.php` — сессионная обёртка: add/set/remove/clear/get/count + сборка строк через БД (Catalog::getProductById).
- `www/includes/order.php` — создание заказа в транзакции (orders + order_items), пересчёт сумм из БД.
- `www/api/cart.php` — JSON-эндпоинт корзины (add/update/remove/get), CSRF.
- `www/cart.php` — страница корзины.
- `www/checkout.php` — страница оформления (форма физ/юр, доставка, оплата) + обработка POST.
- `www/order_success.php` — страница успеха (номер заказа, статус).
- `www/tests/CartLogicTest.php`, `www/tests/CheckoutValidationTest.php` — тесты.
- `www/js/cart.js` — клиентская часть: add-to-cart → POST в api/cart.php, обновление счётчика.

---

## Task 1: Ветка + чистые функции корзины (cart_logic)

**Files:**
- Create: `www/includes/cart_logic.php`
- Test: `www/tests/CartLogicTest.php`

**Interfaces:**
- Consumes: `normalize_quantity()`, `is_valid_quantity()` (`includes/cart_quantity.php`).
- Produces:
  - `cart_build_line(array $product, int $qty): array` — берёт строку товара (с полями id, full_name, price_rub, min_order_qty, qty_step) и желаемое кол-во; нормализует кол-во; возвращает `['product_id'=>int,'name'=>string,'price'=>float,'qty'=>int,'line_total'=>float]`. price = (float)price_rub (0.0 если null). line_total = round(price*qty, 2).
  - `cart_totals(array $lines): array` — `['items_total'=>float,'total_qty'=>int,'positions'=>int]`. items_total = сумма line_total (round 2).
  - `order_number(int $seq, ?int $ts = null): string` — формат `ZP-YYYYMMDD-<seq4>`, где seq дополнен нулями до 4 знаков; ts по умолчанию time().

- [ ] **Step 1: Ветка**

```bash
cd "C:/Users/USER/Documents/Сайт"
git checkout master && git pull
git checkout -b feature/cart-checkout
```

- [ ] **Step 2: Написать падающий тест**

`www/tests/CartLogicTest.php`:

```php
<?php
use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../includes/cart_quantity.php';
require_once __DIR__ . '/../includes/cart_logic.php';

class CartLogicTest extends TestCase
{
    private function product(array $over = []): array
    {
        return array_merge([
            'id' => 1, 'full_name' => 'Пакет 20x15', 'price_rub' => '94.66',
            'min_order_qty' => 100, 'qty_step' => 50,
        ], $over);
    }

    public function testBuildLineNormalizesQtyAndComputesTotal(): void
    {
        $line = cart_build_line($this->product(), 120); // 120 -> 150 (min100 step50)
        $this->assertSame(1, $line['product_id']);
        $this->assertSame('Пакет 20x15', $line['name']);
        $this->assertSame(94.66, $line['price']);
        $this->assertSame(150, $line['qty']);
        $this->assertSame(14199.0, $line['line_total']); // 94.66*150
    }

    public function testBuildLineBelowMinRaises(): void
    {
        $line = cart_build_line($this->product(), 10);
        $this->assertSame(100, $line['qty']);
    }

    public function testBuildLineNullPrice(): void
    {
        $line = cart_build_line($this->product(['price_rub' => null]), 100);
        $this->assertSame(0.0, $line['price']);
        $this->assertSame(0.0, $line['line_total']);
    }

    public function testTotals(): void
    {
        $lines = [
            ['product_id'=>1,'name'=>'A','price'=>94.66,'qty'=>150,'line_total'=>14199.0],
            ['product_id'=>2,'name'=>'B','price'=>10.0,'qty'=>100,'line_total'=>1000.0],
        ];
        $t = cart_totals($lines);
        $this->assertSame(15199.0, $t['items_total']);
        $this->assertSame(250, $t['total_qty']);
        $this->assertSame(2, $t['positions']);
    }

    public function testOrderNumberFormat(): void
    {
        // 2026-06-18 -> ZP-20260618-0007
        $ts = mktime(12, 0, 0, 6, 18, 2026);
        $this->assertSame('ZP-20260618-0007', order_number(7, $ts));
        $this->assertSame('ZP-20260618-1234', order_number(1234, $ts));
    }
}
```

- [ ] **Step 3: Запустить — убедиться, что падает**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests/CartLogicTest.php
```
Expected: FAIL — нет файла cart_logic.php.

- [ ] **Step 4: Реализовать**

`www/includes/cart_logic.php`:

```php
<?php
// Чистые функции корзины/заказа. Без сессии и БД.
require_once __DIR__ . '/cart_quantity.php';

function cart_build_line(array $product, int $qty): array
{
    $min  = (int)($product['min_order_qty'] ?? 1);
    $step = (int)($product['qty_step'] ?? 1);
    $normQty = normalize_quantity($qty, $min, $step);
    $price = isset($product['price_rub']) && $product['price_rub'] !== null
        ? (float)$product['price_rub'] : 0.0;
    return [
        'product_id' => (int)$product['id'],
        'name'       => (string)$product['full_name'],
        'price'      => $price,
        'qty'        => $normQty,
        'line_total' => round($price * $normQty, 2),
    ];
}

function cart_totals(array $lines): array
{
    $itemsTotal = 0.0;
    $totalQty = 0;
    foreach ($lines as $l) {
        $itemsTotal += (float)$l['line_total'];
        $totalQty += (int)$l['qty'];
    }
    return [
        'items_total' => round($itemsTotal, 2),
        'total_qty'   => $totalQty,
        'positions'   => count($lines),
    ];
}

function order_number(int $seq, ?int $ts = null): string
{
    $ts = $ts ?? time();
    return 'ZP-' . date('Ymd', $ts) . '-' . str_pad((string)$seq, 4, '0', STR_PAD_LEFT);
}
```

- [ ] **Step 5: Запустить — зелёное**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS (тесты Плана 1+2 + 5 новых CartLogic).

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/cart_logic.php www/tests/CartLogicTest.php
git commit -m "feat: чистые функции корзины (строка/суммы/номер заказа) с тестами"
```

---

## Task 2: Чистые функции валидации checkout

**Files:**
- Create: `www/includes/checkout_validation.php`
- Test: `www/tests/CheckoutValidationTest.php`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `checkout_validate(array $in): array` — возвращает `['ok'=>bool,'errors'=>array<string,string>,'data'=>array]`. Правила:
    - `customer_name` — обязателен (trim непустой).
    - `phone` — обязателен, формат `/^[\d\s\-\+\(\)]{10,20}$/`.
    - `email` — опционален; если задан — валидный email.
    - `customer_type` ∈ {individual, company} (иначе ошибка).
    - если `company`: `company_name` обязателен; `inn` обязателен и соответствует `/^(\d{10}|\d{12})$/`.
    - `delivery_method` ∈ {pickup, courier, tk}.
    - `payment_method` ∈ {online, invoice}; `invoice` допустим ТОЛЬКО при `company` (иначе ошибка `payment_method`).
    - `data` — очищенный нормализованный набор полей (trim), готовый к вставке.

- [ ] **Step 1: Падающий тест**

`www/tests/CheckoutValidationTest.php`:

```php
<?php
use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../includes/checkout_validation.php';

class CheckoutValidationTest extends TestCase
{
    private function base(array $over = []): array
    {
        return array_merge([
            'customer_name' => 'Иван', 'phone' => '+7 920 346-50-67', 'email' => 'a@b.ru',
            'customer_type' => 'individual', 'delivery_method' => 'pickup',
            'payment_method' => 'online',
        ], $over);
    }

    public function testValidIndividual(): void
    {
        $r = checkout_validate($this->base());
        $this->assertTrue($r['ok']);
        $this->assertSame([], $r['errors']);
    }

    public function testMissingNameAndBadPhone(): void
    {
        $r = checkout_validate($this->base(['customer_name' => '  ', 'phone' => '123']));
        $this->assertFalse($r['ok']);
        $this->assertArrayHasKey('customer_name', $r['errors']);
        $this->assertArrayHasKey('phone', $r['errors']);
    }

    public function testCompanyRequiresCompanyNameAndInn(): void
    {
        $r = checkout_validate($this->base(['customer_type' => 'company']));
        $this->assertFalse($r['ok']);
        $this->assertArrayHasKey('company_name', $r['errors']);
        $this->assertArrayHasKey('inn', $r['errors']);
    }

    public function testCompanyValidWithInn(): void
    {
        $r = checkout_validate($this->base([
            'customer_type' => 'company', 'company_name' => 'ООО Ромашка', 'inn' => '7707083893',
        ]));
        $this->assertTrue($r['ok']);
    }

    public function testInvoiceOnlyForCompany(): void
    {
        $r = checkout_validate($this->base(['payment_method' => 'invoice']));
        $this->assertFalse($r['ok']);
        $this->assertArrayHasKey('payment_method', $r['errors']);
    }

    public function testBadDeliveryAndType(): void
    {
        $r = checkout_validate($this->base(['delivery_method' => 'plane', 'customer_type' => 'x']));
        $this->assertFalse($r['ok']);
        $this->assertArrayHasKey('delivery_method', $r['errors']);
        $this->assertArrayHasKey('customer_type', $r['errors']);
    }
}
```

- [ ] **Step 2: Запустить — FAIL**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests/CheckoutValidationTest.php
```
Expected: FAIL — нет файла.

- [ ] **Step 3: Реализовать**

`www/includes/checkout_validation.php`:

```php
<?php
// Чистая валидация данных оформления заказа. Без БД/сессии.
function checkout_validate(array $in): array
{
    $errors = [];
    $name  = trim((string)($in['customer_name'] ?? ''));
    $phone = trim((string)($in['phone'] ?? ''));
    $email = trim((string)($in['email'] ?? ''));
    $type  = (string)($in['customer_type'] ?? 'individual');
    $delivery = (string)($in['delivery_method'] ?? '');
    $payment  = (string)($in['payment_method'] ?? '');
    $company = trim((string)($in['company_name'] ?? ''));
    $inn = trim((string)($in['inn'] ?? ''));
    $kpp = trim((string)($in['kpp'] ?? ''));
    $legal = trim((string)($in['legal_address'] ?? ''));
    $address = trim((string)($in['delivery_address'] ?? ''));
    $comment = trim((string)($in['comment'] ?? ''));

    if ($name === '') {
        $errors['customer_name'] = 'Укажите имя';
    }
    if (!preg_match('/^[\d\s\-\+\(\)]{10,20}$/', $phone)) {
        $errors['phone'] = 'Укажите корректный телефон';
    }
    if ($email !== '' && !filter_var($email, FILTER_VALIDATE_EMAIL)) {
        $errors['email'] = 'Некорректный email';
    }
    if (!in_array($type, ['individual', 'company'], true)) {
        $errors['customer_type'] = 'Некорректный тип клиента';
    }
    if ($type === 'company') {
        if ($company === '') {
            $errors['company_name'] = 'Укажите название организации';
        }
        if (!preg_match('/^(\d{10}|\d{12})$/', $inn)) {
            $errors['inn'] = 'Укажите корректный ИНН (10 или 12 цифр)';
        }
    }
    if (!in_array($delivery, ['pickup', 'courier', 'tk'], true)) {
        $errors['delivery_method'] = 'Выберите способ доставки';
    }
    if (!in_array($payment, ['online', 'invoice'], true)) {
        $errors['payment_method'] = 'Выберите способ оплаты';
    } elseif ($payment === 'invoice' && $type !== 'company') {
        $errors['payment_method'] = 'Оплата по счёту доступна только юрлицам';
    }

    $data = [
        'customer_type' => $type,
        'customer_name' => $name,
        'phone' => $phone,
        'email' => $email,
        'company_name' => $type === 'company' ? $company : null,
        'inn' => $type === 'company' ? $inn : null,
        'kpp' => $type === 'company' ? ($kpp !== '' ? $kpp : null) : null,
        'legal_address' => $type === 'company' ? ($legal !== '' ? $legal : null) : null,
        'needs_invoice' => !empty($in['needs_invoice']) ? 1 : 0,
        'delivery_method' => $delivery,
        'delivery_address' => $address !== '' ? $address : null,
        'comment' => $comment !== '' ? $comment : null,
        'payment_method' => $payment,
    ];

    return ['ok' => empty($errors), 'errors' => $errors, 'data' => $data];
}
```

- [ ] **Step 4: Запустить — PASS**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS (+6 новых).

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/checkout_validation.php www/tests/CheckoutValidationTest.php
git commit -m "feat: валидация данных оформления заказа (физ/юр, ИНН, способ оплаты) с тестами"
```

---

## Task 3: Сессионная корзина (includes/cart.php)

**Files:**
- Create: `www/includes/cart.php`

**Interfaces:**
- Consumes: `Catalog::getProductById()` (План 2), `cart_build_line()`, `cart_totals()`, сессия (config.php стартует сессию).
- Produces (все работают с `$_SESSION['cart']` = `[product_id=>qty]`):
  - `cart_session_add(int $productId, int $qty): void` — добавляет/увеличивает.
  - `cart_session_set(int $productId, int $qty): void` — устанавливает кол-во; qty<=0 удаляет.
  - `cart_session_remove(int $productId): void`.
  - `cart_session_clear(): void`.
  - `cart_session_raw(): array` — сырой `[id=>qty]`.
  - `cart_session_lines(): array` — массив строк (через getProductById + cart_build_line); пропускает несуществующие/неактивные товары.
  - `cart_session_count(): int` — суммарное кол-во позиций (количество товаров-строк).

- [ ] **Step 1: Реализовать**

`www/includes/cart.php`:

```php
<?php
require_once __DIR__ . '/config.php';
require_once __DIR__ . '/catalog_functions.php';
require_once __DIR__ . '/cart_quantity.php';
require_once __DIR__ . '/cart_logic.php';

function cart_session_raw(): array
{
    return isset($_SESSION['cart']) && is_array($_SESSION['cart']) ? $_SESSION['cart'] : [];
}

function cart_session_add(int $productId, int $qty): void
{
    if ($productId <= 0) { return; }
    $cart = cart_session_raw();
    $cart[$productId] = ($cart[$productId] ?? 0) + max(1, $qty);
    $_SESSION['cart'] = $cart;
}

function cart_session_set(int $productId, int $qty): void
{
    $cart = cart_session_raw();
    if ($qty <= 0) {
        unset($cart[$productId]);
    } else {
        $cart[$productId] = $qty;
    }
    $_SESSION['cart'] = $cart;
}

function cart_session_remove(int $productId): void
{
    $cart = cart_session_raw();
    unset($cart[$productId]);
    $_SESSION['cart'] = $cart;
}

function cart_session_clear(): void
{
    $_SESSION['cart'] = [];
}

function cart_session_lines(): array
{
    $catalog = new Catalog();
    $lines = [];
    foreach (cart_session_raw() as $id => $qty) {
        $product = $catalog->getProductById((int)$id);
        if (!$product) { continue; }
        $lines[] = cart_build_line($product, (int)$qty) + [
            'image_url' => $product['image_url'] ?? '/images/no-image.png',
            'min_order_qty' => (int)($product['min_order_qty'] ?? 1),
            'qty_step' => (int)($product['qty_step'] ?? 1),
        ];
    }
    return $lines;
}

function cart_session_count(): int
{
    return count(cart_session_raw());
}
```

- [ ] **Step 2: Синтаксис**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l includes/cart.php
```
Expected: No syntax errors.

- [ ] **Step 3: Проверка вживую (CLI-сессия)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "require 'includes/cart.php'; cart_session_add(1,120); cart_session_add(2,100); \$lines=cart_session_lines(); echo 'positions='.count(\$lines).PHP_EOL; var_export(cart_totals(\$lines));"
```
Expected: positions=2; items_total>0, total_qty корректен (кол-во нормализовано, напр. 150+...).

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/cart.php
git commit -m "feat: серверная сессионная корзина (add/set/remove/lines/count)"
```

---

## Task 4: Создание заказа (includes/order.php)

**Files:**
- Create: `www/includes/order.php`

**Interfaces:**
- Consumes: `getDbConnection()`, `cart_session_lines()`, `cart_totals()`, `order_number()`, провалидированные данные из `checkout_validate()['data']`.
- Produces:
  - `order_create(array $checkoutData, array $lines): array` — в транзакции: пересчитывает суммы из `$lines` (которые построены из БД), генерирует уникальный `order_number` (на основе следующего seq за сегодня), вставляет в `orders` и `order_items` (snapshot name/price), статус `pending_payment`. Возвращает `['ok'=>bool,'order_id'=>int,'order_number'=>string]` или `['ok'=>false,'error'=>string]`. Если `$lines` пуст — `['ok'=>false,'error'=>'empty_cart']`.

- [ ] **Step 1: Реализовать**

`www/includes/order.php`:

```php
<?php
require_once __DIR__ . '/config.php';
require_once __DIR__ . '/cart_logic.php';

function order_create(array $checkoutData, array $lines): array
{
    if (empty($lines)) {
        return ['ok' => false, 'error' => 'empty_cart'];
    }
    $totals = cart_totals($lines);
    $db = getDbConnection();
    try {
        $db->beginTransaction();

        // Следующий порядковый номер за сегодня
        $countStmt = $db->query("SELECT COUNT(*) FROM orders WHERE DATE(created_at) = CURDATE()");
        $seq = (int)$countStmt->fetchColumn() + 1;
        $orderNo = order_number($seq);

        $stmt = $db->prepare("INSERT INTO orders
            (order_number, status, customer_type, customer_name, phone, email,
             company_name, inn, kpp, legal_address, needs_invoice,
             delivery_method, delivery_address, comment, payment_method,
             items_total, total)
            VALUES
            (:order_number, 'pending_payment', :customer_type, :customer_name, :phone, :email,
             :company_name, :inn, :kpp, :legal_address, :needs_invoice,
             :delivery_method, :delivery_address, :comment, :payment_method,
             :items_total, :total)");
        $stmt->execute([
            ':order_number' => $orderNo,
            ':customer_type' => $checkoutData['customer_type'],
            ':customer_name' => $checkoutData['customer_name'],
            ':phone' => $checkoutData['phone'],
            ':email' => $checkoutData['email'] !== '' ? $checkoutData['email'] : null,
            ':company_name' => $checkoutData['company_name'],
            ':inn' => $checkoutData['inn'],
            ':kpp' => $checkoutData['kpp'],
            ':legal_address' => $checkoutData['legal_address'],
            ':needs_invoice' => $checkoutData['needs_invoice'],
            ':delivery_method' => $checkoutData['delivery_method'],
            ':delivery_address' => $checkoutData['delivery_address'],
            ':comment' => $checkoutData['comment'],
            ':payment_method' => $checkoutData['payment_method'],
            ':items_total' => $totals['items_total'],
            ':total' => $totals['items_total'], // доставка согласуется отдельно
        ]);
        $orderId = (int)$db->lastInsertId();

        $itemStmt = $db->prepare("INSERT INTO order_items
            (order_id, product_id, name_snapshot, price_snapshot, qty, line_total)
            VALUES (:order_id, :product_id, :name, :price, :qty, :line_total)");
        foreach ($lines as $l) {
            $itemStmt->execute([
                ':order_id' => $orderId,
                ':product_id' => $l['product_id'],
                ':name' => $l['name'],
                ':price' => $l['price'],
                ':qty' => $l['qty'],
                ':line_total' => $l['line_total'],
            ]);
        }

        $db->commit();
        return ['ok' => true, 'order_id' => $orderId, 'order_number' => $orderNo];
    } catch (Throwable $e) {
        if ($db->inTransaction()) { $db->rollBack(); }
        error_log('order_create error: ' . $e->getMessage());
        return ['ok' => false, 'error' => 'db_error'];
    }
}
```

- [ ] **Step 2: Синтаксис**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l includes/order.php
```
Expected: No syntax errors.

- [ ] **Step 3: Проверка вживую (создаёт реальный заказ в локальной БД)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "
require 'includes/cart.php'; require 'includes/order.php';
cart_session_add(1,150); \$lines=cart_session_lines();
\$data=['customer_type'=>'individual','customer_name'=>'Тест','phone'=>'+79991234567','email'=>'t@t.ru','company_name'=>null,'inn'=>null,'kpp'=>null,'legal_address'=>null,'needs_invoice'=>0,'delivery_method'=>'pickup','delivery_address'=>null,'comment'=>null,'payment_method'=>'online'];
\$r=order_create(\$data,\$lines); var_export(\$r);"
```
Expected: `['ok'=>true,'order_id'=>N,'order_number'=>'ZP-...']`.
Затем проверить в БД:
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="SELECT id,order_number,status,total FROM orders ORDER BY id DESC LIMIT 1; SELECT order_id,product_id,name_snapshot,price_snapshot,qty,line_total FROM order_items ORDER BY id DESC LIMIT 1;"
```
Expected: заказ со статусом `pending_payment` и позиция со snapshot.
Очистить тестовый заказ:
```bash
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="DELETE oi FROM order_items oi JOIN orders o ON o.id=oi.order_id WHERE o.customer_name='Тест'; DELETE FROM orders WHERE customer_name='Тест';"
```

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/order.php
git commit -m "feat: создание заказа в транзакции (orders + order_items, snapshot цен)"
```

---

## Task 5: JSON-эндпоинт корзины (api/cart.php) + cart.js

**Files:**
- Create: `www/api/cart.php`
- Create: `www/js/cart.js`

**Interfaces:**
- Consumes: `includes/cart.php`, CSRF (`generateCsrfToken`/`verifyCsrfToken`), `cart_totals`.
- Produces: POST JSON-эндпоинт `api/cart.php` с полем `action` ∈ {add, update, remove, get}; защита CSRF (поле `csrf_token`); возвращает `{success, count, items_total, positions}`. `cart.js`: перехватывает `.add-to-cart`, шлёт add, обновляет `.cart-counter`.

- [ ] **Step 1: Реализовать api/cart.php**

`www/api/cart.php`:

```php
<?php
require_once __DIR__ . '/../includes/cart.php';
header('Content-Type: application/json; charset=utf-8');

$action = $_POST['action'] ?? $_GET['action'] ?? '';

// CSRF для мутаций
if (in_array($action, ['add', 'update', 'remove'], true)) {
    $token = $_POST['csrf_token'] ?? '';
    if (!verifyCsrfToken($token)) {
        http_response_code(403);
        echo json_encode(['success' => false, 'message' => 'CSRF']);
        exit;
    }
}

$id = (int)($_POST['id'] ?? 0);
$qty = (int)($_POST['qty'] ?? 1);

switch ($action) {
    case 'add':    cart_session_add($id, $qty); break;
    case 'update': cart_session_set($id, $qty); break;
    case 'remove': cart_session_remove($id); break;
    case 'get':    break;
    default:
        http_response_code(400);
        echo json_encode(['success' => false, 'message' => 'bad action']);
        exit;
}

$lines = cart_session_lines();
$totals = cart_totals($lines);
echo json_encode([
    'success' => true,
    'count' => $totals['positions'],
    'total_qty' => $totals['total_qty'],
    'items_total' => $totals['items_total'],
]);
```

- [ ] **Step 2: Реализовать js/cart.js**

`www/js/cart.js`:

```javascript
// Серверная корзина: добавление и счётчик.
(function () {
  function csrf() {
    var m = document.querySelector('meta[name="csrf-token"]');
    return m ? m.getAttribute('content') : '';
  }
  function refreshCounter(count) {
    document.querySelectorAll('.cart-counter').forEach(function (c) {
      c.textContent = count;
      c.style.display = count > 0 ? 'flex' : 'none';
    });
  }
  function post(params) {
    var body = new URLSearchParams(params);
    return fetch('/api/cart.php', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: body.toString(),
    }).then(function (r) { return r.json(); });
  }
  document.addEventListener('DOMContentLoaded', function () {
    post({ action: 'get' }).then(function (d) { if (d.success) refreshCounter(d.count); });
    document.querySelectorAll('.add-to-cart').forEach(function (btn) {
      btn.addEventListener('click', function (e) {
        e.preventDefault();
        var id = btn.dataset.id;
        var qtyInput = document.getElementById('qty');
        var qty = qtyInput ? qtyInput.value : (btn.dataset.min || 1);
        post({ action: 'add', id: id, qty: qty, csrf_token: csrf() }).then(function (d) {
          if (d.success) {
            refreshCounter(d.count);
            if (typeof ym !== 'undefined') { ym(106644271, 'reachGoal', 'add_to_cart'); }
          }
        });
      });
    });
  });
})();
```

- [ ] **Step 3: Синтаксис PHP + проверка эндпоинта (CSRF-флоу пропускаем в CLI)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l api/cart.php
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['action'=>'get']; require 'api/cart.php';"
```
Expected: No syntax errors; JSON `{"success":true,...}`.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/api/cart.php www/js/cart.js
git commit -m "feat: JSON-эндпоинт корзины (CSRF) + cart.js (добавление, счётчик)"
```

---

## Task 6: Страница корзины (cart.php)

**Files:**
- Create: `www/cart.php`

**Interfaces:**
- Consumes: `includes/cart.php`, `includes/product_view.php`, `generateCsrfToken()`, header/footer.
- Produces: страница `/cart.php` — список позиций (фото, имя, цена, кол-во с min/step, сумма строки), итог, кнопки изменения/удаления (через cart.js/api), кнопка «Оформить заказ» → /checkout.php; пустое состояние.

- [ ] **Step 1: Реализовать cart.php**

`www/cart.php`:

```php
<?php
require_once __DIR__ . '/includes/cart.php';
require_once __DIR__ . '/includes/product_view.php';
$lines = cart_session_lines();
$totals = cart_totals($lines);
$csrf = generateCsrfToken();
?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<?= htmlspecialchars($csrf) ?>">
    <title>Корзина | ZLOCK</title>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/catalog.css">
</head>
<body>
<div class="site-wrapper">
    <?php include __DIR__ . '/header.php'; ?>
    <main class="main-content">
        <section class="catalog-section"><div class="container">
            <h1>Корзина</h1>
            <?php if (empty($lines)): ?>
                <div class="no-products">
                    <i class="fas fa-shopping-cart"></i>
                    <p>Корзина пуста.</p>
                    <a href="/katalog_zip_paketov" class="btn btn-primary">В каталог</a>
                </div>
            <?php else: ?>
                <table class="cart-table" style="width:100%;border-collapse:collapse">
                    <tbody>
                    <?php foreach ($lines as $l): ?>
                        <tr data-id="<?= (int)$l['product_id'] ?>" style="border-bottom:1px solid #eef2f6">
                            <td style="padding:10px;width:60px">
                                <img src="<?= htmlspecialchars($l['image_url']) ?>" alt="" style="width:48px;height:48px;object-fit:contain;background:#f8fafc;border-radius:8px">
                            </td>
                            <td style="padding:10px">
                                <div><?= htmlspecialchars($l['name']) ?></div>
                                <small style="color:#94a3b8"><?= htmlspecialchars(pv_pack_note($l['min_order_qty'], $l['qty_step'])) ?></small>
                            </td>
                            <td style="padding:10px;white-space:nowrap"><?= $l['price'] > 0 ? pv_format_price($l['price']) : 'по запросу' ?></td>
                            <td style="padding:10px">
                                <input type="number" class="cart-qty filter-input small" value="<?= (int)$l['qty'] ?>" min="<?= (int)$l['min_order_qty'] ?>" step="<?= (int)$l['qty_step'] ?>" data-id="<?= (int)$l['product_id'] ?>" style="width:100px">
                            </td>
                            <td style="padding:10px;white-space:nowrap;font-weight:700"><?= pv_format_price($l['line_total']) ?></td>
                            <td style="padding:10px">
                                <button class="cart-remove btn btn-outline btn-sm" data-id="<?= (int)$l['product_id'] ?>"><i class="fas fa-trash"></i></button>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
                <div style="display:flex;justify-content:space-between;align-items:center;margin-top:20px;flex-wrap:wrap;gap:12px">
                    <div style="font-size:1.2rem">Итого: <b><?= pv_format_price($totals['items_total']) ?></b> <small style="color:#94a3b8">(доставка согласуется отдельно)</small></div>
                    <a href="/checkout.php" class="btn btn-primary"><i class="fas fa-arrow-right"></i> Оформить заказ</a>
                </div>
            <?php endif; ?>
        </div></section>
    </main>
    <?php include __DIR__ . '/footer.php'; ?>
</div>
<script src="/js/cart.js"></script>
<script>
// Изменение количества и удаление на странице корзины
document.addEventListener('DOMContentLoaded', function () {
  var csrf = document.querySelector('meta[name="csrf-token"]').getAttribute('content');
  function send(action, id, qty) {
    var body = new URLSearchParams({ action: action, id: id, qty: qty || 0, csrf_token: csrf });
    return fetch('/api/cart.php', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: body.toString() })
      .then(function (r) { return r.json(); }).then(function () { location.reload(); });
  }
  document.querySelectorAll('.cart-qty').forEach(function (inp) {
    inp.addEventListener('change', function () { send('update', inp.dataset.id, inp.value); });
  });
  document.querySelectorAll('.cart-remove').forEach(function (b) {
    b.addEventListener('click', function () { send('remove', b.dataset.id, 0); });
  });
});
</script>
</body>
</html>
```

- [ ] **Step 2: Синтаксис + рендер**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l cart.php
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "require 'cart.php';" | grep -E "Корзина пуста|Оформить заказ" | head -1
```
Expected: нет ошибок; для пустой корзины — «Корзина пуста».

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/cart.php
git commit -m "feat: страница корзины (позиции, кол-во, удаление, итог)"
```

---

## Task 7: Оформление (checkout.php) + страница успеха (order_success.php)

**Files:**
- Create: `www/checkout.php`
- Create: `www/order_success.php`

**Interfaces:**
- Consumes: `includes/cart.php`, `includes/checkout_validation.php`, `includes/order.php`, CSRF, header/footer.
- Produces: GET — форма оформления (физ/юр переключатель, доставка, способ оплаты); POST — CSRF + `checkout_validate`; при успехе `order_create`, очистка корзины, redirect на `/order_success.php?order=<number>`. order_success — показывает номер и статус «ожидает оплаты» (оплата — План 4).

- [ ] **Step 1: Реализовать checkout.php**

`www/checkout.php`:

```php
<?php
require_once __DIR__ . '/includes/cart.php';
require_once __DIR__ . '/includes/checkout_validation.php';
require_once __DIR__ . '/includes/order.php';

$errors = [];
$old = [];
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if (!verifyCsrfToken($_POST['csrf_token'] ?? '')) {
        $errors['_'] = 'Сессия истекла, обновите страницу';
    } else {
        $old = $_POST;
        $v = checkout_validate($_POST);
        $lines = cart_session_lines();
        if (empty($lines)) {
            $errors['_'] = 'Корзина пуста';
        } elseif (!$v['ok']) {
            $errors = $v['errors'];
        } else {
            $res = order_create($v['data'], $lines);
            if ($res['ok']) {
                cart_session_clear();
                redirect('/order_success.php?order=' . urlencode($res['order_number']));
            } else {
                $errors['_'] = $res['error'] === 'empty_cart' ? 'Корзина пуста' : 'Ошибка оформления, попробуйте позже';
            }
        }
    }
}

$lines = cart_session_lines();
$totals = cart_totals($lines);
$csrf = generateCsrfToken();
function old_val(array $old, string $k): string { return htmlspecialchars((string)($old[$k] ?? '')); }
?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Оформление заказа | ZLOCK</title>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/catalog.css">
</head>
<body>
<div class="site-wrapper">
    <?php include __DIR__ . '/header.php'; ?>
    <main class="main-content">
        <section class="catalog-section"><div class="container">
            <h1>Оформление заказа</h1>
            <?php if (empty($lines)): ?>
                <div class="no-products"><p>Корзина пуста.</p><a href="/katalog_zip_paketov" class="btn btn-primary">В каталог</a></div>
            <?php else: ?>
                <?php if (!empty($errors['_'])): ?>
                    <div class="form-error" style="color:#dc2626;margin-bottom:12px"><?= htmlspecialchars($errors['_']) ?></div>
                <?php endif; ?>
                <form method="POST" action="/checkout.php" style="max-width:560px">
                    <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf) ?>">

                    <div style="display:flex;gap:8px;margin-bottom:14px">
                        <label><input type="radio" name="customer_type" value="individual" <?= (($old['customer_type'] ?? 'individual') === 'individual') ? 'checked' : '' ?>> Физлицо</label>
                        <label><input type="radio" name="customer_type" value="company" <?= (($old['customer_type'] ?? '') === 'company') ? 'checked' : '' ?>> Юрлицо</label>
                    </div>

                    <div class="form-group"><input type="text" name="customer_name" placeholder="Имя *" value="<?= old_val($old,'customer_name') ?>"></div>
                    <?php if (!empty($errors['customer_name'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['customer_name']) ?></small><?php endif; ?>
                    <div class="form-group"><input type="tel" name="phone" placeholder="Телефон *" value="<?= old_val($old,'phone') ?>"></div>
                    <?php if (!empty($errors['phone'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['phone']) ?></small><?php endif; ?>
                    <div class="form-group"><input type="email" name="email" placeholder="Email" value="<?= old_val($old,'email') ?>"></div>
                    <?php if (!empty($errors['email'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['email']) ?></small><?php endif; ?>

                    <div id="companyFields" style="border:1px dashed #c9d6e5;border-radius:8px;padding:10px;margin:10px 0">
                        <div class="form-group"><input type="text" name="company_name" placeholder="Организация" value="<?= old_val($old,'company_name') ?>"></div>
                        <?php if (!empty($errors['company_name'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['company_name']) ?></small><?php endif; ?>
                        <div class="form-group"><input type="text" name="inn" placeholder="ИНН" value="<?= old_val($old,'inn') ?>"></div>
                        <?php if (!empty($errors['inn'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['inn']) ?></small><?php endif; ?>
                        <div class="form-group"><input type="text" name="kpp" placeholder="КПП" value="<?= old_val($old,'kpp') ?>"></div>
                        <div class="form-group"><input type="text" name="legal_address" placeholder="Юр. адрес" value="<?= old_val($old,'legal_address') ?>"></div>
                        <label><input type="checkbox" name="needs_invoice" value="1" <?= !empty($old['needs_invoice']) ? 'checked' : '' ?>> Нужен счёт</label>
                    </div>

                    <div class="form-group">
                        <label>Доставка:</label>
                        <select name="delivery_method">
                            <option value="pickup" <?= (($old['delivery_method'] ?? '')==='pickup')?'selected':'' ?>>Самовывоз</option>
                            <option value="courier" <?= (($old['delivery_method'] ?? '')==='courier')?'selected':'' ?>>Курьер</option>
                            <option value="tk" <?= (($old['delivery_method'] ?? '')==='tk')?'selected':'' ?>>Транспортная компания</option>
                        </select>
                    </div>
                    <?php if (!empty($errors['delivery_method'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['delivery_method']) ?></small><?php endif; ?>
                    <div class="form-group"><input type="text" name="delivery_address" placeholder="Адрес доставки" value="<?= old_val($old,'delivery_address') ?>"></div>
                    <div class="form-group"><textarea name="comment" placeholder="Комментарий" rows="2"><?= old_val($old,'comment') ?></textarea></div>

                    <div class="form-group">
                        <label>Оплата:</label>
                        <label><input type="radio" name="payment_method" value="online" <?= (($old['payment_method'] ?? 'online')==='online')?'checked':'' ?>> Картой онлайн</label>
                        <label id="invoiceOpt" style="opacity:.5"><input type="radio" name="payment_method" value="invoice" <?= (($old['payment_method'] ?? '')==='invoice')?'checked':'' ?>> По счёту (для юрлиц)</label>
                    </div>
                    <?php if (!empty($errors['payment_method'])): ?><small style="color:#dc2626"><?= htmlspecialchars($errors['payment_method']) ?></small><?php endif; ?>

                    <div style="margin:14px 0;font-size:1.1rem">К оплате: <b><?= pv_format_price($totals['items_total']) ?></b></div>
                    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Оформить заказ</button>
                    <p style="font-size:0.8rem;color:#94a3b8;margin-top:8px">Нажимая кнопку, вы соглашаетесь с <a href="/polconf.html">политикой конфиденциальности</a>.</p>
                </form>
            <?php endif; ?>
        </div></section>
    </main>
    <?php include __DIR__ . '/footer.php'; ?>
</div>
<script>
// Показ полей юрлица и доступности оплаты по счёту
document.addEventListener('DOMContentLoaded', function () {
  var radios = document.querySelectorAll('input[name="customer_type"]');
  var company = document.getElementById('companyFields');
  var invoiceOpt = document.getElementById('invoiceOpt');
  var invoiceRadio = invoiceOpt ? invoiceOpt.querySelector('input') : null;
  function sync() {
    var isCompany = document.querySelector('input[name="customer_type"]:checked').value === 'company';
    company.style.display = isCompany ? 'block' : 'none';
    invoiceOpt.style.opacity = isCompany ? '1' : '.5';
    if (invoiceRadio) invoiceRadio.disabled = !isCompany;
    if (!isCompany && invoiceRadio && invoiceRadio.checked) {
      document.querySelector('input[name="payment_method"][value="online"]').checked = true;
    }
  }
  radios.forEach(function (r) { r.addEventListener('change', sync); });
  sync();
});
</script>
</body>
</html>
```

- [ ] **Step 2: Реализовать order_success.php**

`www/order_success.php`:

```php
<?php
require_once __DIR__ . '/includes/config.php';
$orderNumber = isset($_GET['order']) ? preg_replace('/[^A-Za-z0-9\-]/', '', $_GET['order']) : '';
?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Заказ оформлен | ZLOCK</title>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="site-wrapper">
    <?php include __DIR__ . '/header.php'; ?>
    <main class="main-content">
        <section class="catalog-section"><div class="container" style="text-align:center;padding:40px 0">
            <i class="fas fa-check-circle" style="font-size:3rem;color:#10b981"></i>
            <h1>Заказ оформлен</h1>
            <?php if ($orderNumber): ?>
                <p>Номер заказа: <b><?= htmlspecialchars($orderNumber) ?></b></p>
            <?php endif; ?>
            <p>Статус: ожидает оплаты. Мы свяжемся с вами для подтверждения и согласования доставки.</p>
            <a href="/katalog_zip_paketov" class="btn btn-primary">Продолжить покупки</a>
        </div></section>
    </main>
    <?php include __DIR__ . '/footer.php'; ?>
</div>
</body>
</html>
```

- [ ] **Step 3: Синтаксис + проверка пустой корзины (GET)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l checkout.php && "C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -l order_success.php
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['order'=>'ZP-20260618-0001']; require 'order_success.php';" | grep "ZP-20260618-0001" | head -1
```
Expected: нет ошибок; страница успеха содержит номер заказа.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/checkout.php www/order_success.php
git commit -m "feat: оформление заказа (физ/юр, доставка, оплата, CSRF) + страница успеха"
```

---

## Task 8: Связки в UI (счётчик корзины, ссылки, подключение cart.js)

**Files:**
- Modify: `www/header.php` (иконка/счётчик корзины + ссылка на /cart.php; meta csrf-token)
- Modify: `www/product.php` (подключить cart.js, добавить meta csrf-token)
- Modify: `www/katalog_zip_paketov/katalog.php` (подключить cart.js, meta csrf-token)

**Interfaces:**
- Consumes: `generateCsrfToken()` (config.php уже подключён на этих страницах), `cart.js`.
- Produces: на публичных страницах есть `<meta name="csrf-token">`, иконка корзины со счётчиком (ссылка `/cart.php`), подключён `cart.js`; кнопки `.add-to-cart` работают через сервер.

- [ ] **Step 1: header.php — ссылка на корзину со счётчиком**

В `www/header.php` рядом с кнопкой «Заказать звонок» (в `.header-content`) добавить ссылку на корзину:

```php
            <a href="/cart.php" class="cart-link" style="position:relative;display:inline-flex;align-items:center;margin-left:10px" aria-label="Корзина">
                <i class="fas fa-shopping-cart" style="font-size:1.3rem"></i>
                <span class="cart-counter" style="display:none;position:absolute;top:-8px;right:-10px;background:#ffb020;color:#0b1f3a;border-radius:999px;min-width:18px;height:18px;font-size:0.7rem;font-weight:700;align-items:center;justify-content:center;padding:0 4px">0</span>
            </a>
```

- [ ] **Step 2: meta csrf-token + cart.js на страницах**

В `www/product.php` и `www/katalog_zip_paketov/katalog.php` в `<head>` добавить (используется `generateCsrfToken()` из config.php, который уже подключён):
```php
    <meta name="csrf-token" content="<?= htmlspecialchars(generateCsrfToken()) ?>">
```
И перед `</body>` подключить cart.js (после существующих скриптов):
```php
    <script src="/js/cart.js"></script>
```
В `www/header.php` сам по себе скрипт не подключаем (он на страницах). Если на странице уже есть meta csrf (cart.php/checkout.php) — не дублировать.

- [ ] **Step 3: Синтаксис**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
PHP="C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe"
"$PHP" -l header.php && "$PHP" -l product.php && "$PHP" -l katalog_zip_paketov/katalog.php
```
Expected: No syntax errors.

- [ ] **Step 4: Рендер-проверка наличия счётчика и cart.js**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "\$_GET=['id'=>1]; require 'product.php';" | grep -E "cart-counter|/js/cart.js|csrf-token" | head -3
```
Expected: найдены cart-counter, /js/cart.js, csrf-token.

- [ ] **Step 5: Регрессионные тесты**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS (все тесты).

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/header.php www/product.php www/katalog_zip_paketov/katalog.php
git commit -m "feat: иконка корзины со счётчиком + подключение серверной корзины на страницах"
```

---

## Task 9: Финальные гейты Плана 3

**Files:** —

- [ ] **Step 1: Все тесты**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" vendor/phpunit/phpunit/phpunit --bootstrap tests/bootstrap.php tests
```
Expected: PASS — Плана 1+2 (15) + CartLogic (5) + CheckoutValidation (6).

- [ ] **Step 2: Линт новых/изменённых PHP**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
PHP="C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe"
for f in includes/cart_logic.php includes/checkout_validation.php includes/cart.php includes/order.php api/cart.php cart.php checkout.php order_success.php product.php katalog_zip_paketov/katalog.php header.php; do "$PHP" -l "$f"; done
```
Expected: No syntax errors для каждого.

- [ ] **Step 3: Сквозной сценарий вживую (корзина → заказ → очистка)**

```bash
cd "C:/Users/USER/Documents/Сайт/www"
"C:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe" -r "
require 'includes/cart.php'; require 'includes/checkout_validation.php'; require 'includes/order.php';
cart_session_add(1,150); cart_session_add(2,100);
\$v=checkout_validate(['customer_name'=>'Тест2','phone'=>'+79990001122','customer_type'=>'individual','delivery_method'=>'tk','payment_method'=>'online']);
\$r=\$v['ok'] ? order_create(\$v['data'], cart_session_lines()) : ['ok'=>false,'errors'=>\$v['errors']];
var_export(\$r);"
"C:/laragon/bin/mysql/mysql-8.4.3-winx64/bin/mysql.exe" -u root -h 127.0.0.1 c103264_zippaket_optom_ru --execute="SELECT order_number,status,items_total FROM orders WHERE customer_name='Тест2'; DELETE oi FROM order_items oi JOIN orders o ON o.id=oi.order_id WHERE o.customer_name='Тест2'; DELETE FROM orders WHERE customer_name='Тест2';"
```
Expected: `ok=>true` с order_number; в БД заказ `pending_payment` с 2 позициями; затем удалён.

- [ ] **Step 4: Чистота дерева**

```bash
cd "C:/Users/USER/Documents/Сайт"
git status -s
```
Expected: пусто.

---

## Self-Review (выполнено при написании плана)

- **Покрытие spec:** серверная сессионная корзина — Tasks 3/5/6; валидация мин/кратность — `cart_build_line` через normalize_quantity (Task 1); checkout физ/юр + доставка + способ оплаты + invoice-только-юрлицам — Task 2 (валидация) + Task 7 (форма); создание заказа orders/order_items со snapshot — Task 4; серверный пересчёт сумм (не доверяем клиенту) — Task 4 (`order_create` берёт суммы из lines, построенных из БД); CSRF — Tasks 5/7; reCAPTCHA — на checkout (примечание ниже); номер заказа — Task 1. Онлайн-оплата (редирект ЮKassa) — План 4; уведомления amoCRM/Telegram/email — План 5 (вне объёма).
- **reCAPTCHA на checkout:** интеграция reCAPTCHA v3 как на других формах — отнесена к Плану 5/доработке форм, чтобы не раздувать Plan 3; CSRF на checkout присутствует. Если требуется немедленно — добавить grecaptcha.execute в форму и серверную проверку токена (есть RECAPTCHA_SECRET_KEY).
- **Плейсхолдеры:** нет; код-шаги содержат полный код.
- **Согласованность типов:** `cart_build_line`/`cart_totals`/`order_number` (Task 1) используются в Tasks 3/4; `checkout_validate(...)['data']` (Task 2) подаётся в `order_create` (Task 4) — поля совпадают со схемой orders (Плана 1); сессионные функции (Task 3) используются в api/cart.php (Task 5), cart.php (Task 6), checkout.php (Task 7).
