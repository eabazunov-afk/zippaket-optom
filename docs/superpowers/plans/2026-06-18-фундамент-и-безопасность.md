# Фундамент и безопасность — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Подготовить безопасную основу для интернет-магазина: убрать секреты из кода, починить баг URL, развернуть схему БД для заказов и тест-харнес с первой проверенной бизнес-логикой.

**Architecture:** Расширяем текущий процедурный PHP-сайт (Вариант A из spec). Секреты выносятся в отдельный незакоммиченный `config.php` с шаблоном-примером. Новые таблицы и поля добавляются SQL-миграцией. Тестируемая бизнес-логика (валидация количеств, статусы заказа) выносится в чистые функции, покрытые PHPUnit.

**Tech Stack:** PHP 7.4+/8.x, PDO/MySQL (MariaDB), Composer, PHPUnit 9, ЮKassa (интерфейс на этом этапе без реализации).

## Global Constraints

- Стек — PHP «на месте», процедурный стиль + PDO, как в существующем коде. Без фреймворков.
- Секреты НЕ попадают в git: `www/includes/config.php`, `www/tg/config.php` уже в `.gitignore`.
- Все запросы к БД — подготовленные выражения PDO (как в `getDbConnection()`).
- Цена/название позиций копируются в `order_items` (snapshot) на момент заказа.
- Статусы заказа: `new` → `pending_payment` → `paid` → `processing` → `shipped` → `done`; либо `canceled`.
- Кодировка БД: `utf8mb4`. Таймзона: `Europe/Moscow`.
- Платёжный провайдер за интерфейсом `PaymentGateway` (сменяемость).
- Работаем в feature-ветке; коммит после каждого пройденного шага/гейта.

---

## File Structure

- `www/composer.json` — добавить dev-зависимость PHPUnit и autoload для тестов.
- `www/tests/bootstrap.php` — автозагрузка для тестов (Composer + подключение тестируемых файлов).
- `www/tests/CartQuantityTest.php` — тесты валидации количеств.
- `www/tests/OrderStatusTest.php` — тесты статусной машины заказа.
- `www/includes/cart_quantity.php` — чистые функции нормализации количества (без БД/сессии).
- `www/includes/order_status.php` — чистые функции переходов статусов заказа.
- `www/includes/payment/PaymentGateway.php` — интерфейс платёжного шлюза.
- `www/includes/config.example.php` — шаблон конфига без секретов (коммитится).
- `www/includes/config.php` — реальные секреты (НЕ коммитится; правим только баг `SITE_URL`).
- `db/migrations/2026-06-18-orders-schema.sql` — миграция: новые поля `products`, таблицы `orders`, `order_items`.
- `db/README.md` — как применять миграции.

---

## Task 1: Ветка и тест-харнес (PHPUnit)

**Files:**
- Modify: `www/composer.json`
- Create: `www/tests/bootstrap.php`

**Interfaces:**
- Consumes: ничего.
- Produces: рабочий `composer test` → `vendor/bin/phpunit`; bootstrap, подключающий `vendor/autoload.php`.

- [ ] **Step 1: Создать feature-ветку**

```bash
cd "C:/Users/USER/Documents/Сайт"
git checkout -b feature/foundation-security
```

- [ ] **Step 2: Добавить PHPUnit в composer.json**

Заменить содержимое `www/composer.json` на:

```json
{
    "require": {
        "phpoffice/phpspreadsheet": "^1.30"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit --bootstrap tests/bootstrap.php tests"
    }
}
```

- [ ] **Step 3: Создать bootstrap тестов**

`www/tests/bootstrap.php`:

```php
<?php
// Автозагрузка Composer + тестируемые модули (чистые функции, без БД).
require_once __DIR__ . '/../vendor/autoload.php';
require_once __DIR__ . '/../includes/cart_quantity.php';
require_once __DIR__ . '/../includes/order_status.php';
```

- [ ] **Step 4: Установить зависимости**

Run (из `www/`):
```bash
cd "C:/Users/USER/Documents/Сайт/www" && php composer update
```
Expected: создаётся/обновляется `vendor/`, появляется `vendor/bin/phpunit`. (Если `php` нет в PATH — указать полный путь к PHP CLI.)

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/composer.json www/composer.lock www/tests/bootstrap.php
git commit -m "chore: добавить PHPUnit и bootstrap для тестов"
```

---

## Task 2: Валидация количества (мин/кратность)

**Files:**
- Create: `www/includes/cart_quantity.php`
- Test: `www/tests/CartQuantityTest.php`

**Interfaces:**
- Consumes: ничего (чистые функции).
- Produces:
  - `normalize_quantity(int $qty, int $min, int $step): int` — возвращает корректное количество: не меньше `$min`, округлённое ВВЕРХ до кратного `$step` относительно `$min`. При `$step <= 0` трактуется как 1. При `$min <= 0` трактуется как `$step`.
  - `is_valid_quantity(int $qty, int $min, int $step): bool` — true, если `$qty >= $min` и `($qty - $min) % $step === 0`.

- [ ] **Step 1: Написать падающий тест**

`www/tests/CartQuantityTest.php`:

```php
<?php
use PHPUnit\Framework\TestCase;

class CartQuantityTest extends TestCase
{
    public function testBelowMinimumRaisesToMinimum(): void
    {
        $this->assertSame(100, normalize_quantity(30, 100, 50));
    }

    public function testRoundsUpToStepFromMinimum(): void
    {
        // min=100, step=50: допустимы 100,150,200... 120 -> 150
        $this->assertSame(150, normalize_quantity(120, 100, 50));
    }

    public function testExactValidValueUnchanged(): void
    {
        $this->assertSame(200, normalize_quantity(200, 100, 50));
    }

    public function testStepZeroTreatedAsOne(): void
    {
        $this->assertSame(137, normalize_quantity(137, 1, 0));
    }

    public function testIsValidQuantity(): void
    {
        $this->assertTrue(is_valid_quantity(150, 100, 50));
        $this->assertFalse(is_valid_quantity(120, 100, 50));
        $this->assertFalse(is_valid_quantity(50, 100, 50));
    }
}
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `www/`):
```bash
php composer test
```
Expected: FAIL — `Error: Call to undefined function normalize_quantity()`.

- [ ] **Step 3: Реализовать функции**

`www/includes/cart_quantity.php`:

```php
<?php
/**
 * Нормализует количество товара под минимум и кратность упаковке.
 * Округляет ВВЕРХ до ближайшего допустимого: min, min+step, min+2*step, ...
 */
function normalize_quantity(int $qty, int $min, int $step): int
{
    if ($step <= 0) {
        $step = 1;
    }
    if ($min <= 0) {
        $min = $step;
    }
    if ($qty <= $min) {
        return $min;
    }
    $over = $qty - $min;
    $remainder = $over % $step;
    if ($remainder === 0) {
        return $qty;
    }
    return $qty + ($step - $remainder);
}

/**
 * Проверяет, что количество допустимо без изменения.
 */
function is_valid_quantity(int $qty, int $min, int $step): bool
{
    if ($step <= 0) {
        $step = 1;
    }
    if ($min <= 0) {
        $min = $step;
    }
    return $qty >= $min && (($qty - $min) % $step) === 0;
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run (из `www/`):
```bash
php composer test
```
Expected: PASS (5 assertions в CartQuantityTest зелёные).

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/cart_quantity.php www/tests/CartQuantityTest.php
git commit -m "feat: валидация количества товара (мин/кратность) с тестами"
```

---

## Task 3: Статусная машина заказа

**Files:**
- Create: `www/includes/order_status.php`
- Test: `www/tests/OrderStatusTest.php`

**Interfaces:**
- Consumes: ничего (чистые функции).
- Produces:
  - `order_statuses(): array` — список валидных статусов: `['new','pending_payment','paid','processing','shipped','done','canceled']`.
  - `can_transition(string $from, string $to): bool` — разрешён ли переход. Разрешённые переходы:
    - `new` → `pending_payment`, `canceled`
    - `pending_payment` → `paid`, `canceled`
    - `paid` → `processing`, `canceled`
    - `processing` → `shipped`, `canceled`
    - `shipped` → `done`
    - `done` → (нет)
    - `canceled` → (нет)

- [ ] **Step 1: Написать падающий тест**

`www/tests/OrderStatusTest.php`:

```php
<?php
use PHPUnit\Framework\TestCase;

class OrderStatusTest extends TestCase
{
    public function testValidStatusesList(): void
    {
        $this->assertContains('pending_payment', order_statuses());
        $this->assertContains('paid', order_statuses());
        $this->assertCount(7, order_statuses());
    }

    public function testAllowedTransitions(): void
    {
        $this->assertTrue(can_transition('new', 'pending_payment'));
        $this->assertTrue(can_transition('pending_payment', 'paid'));
        $this->assertTrue(can_transition('paid', 'processing'));
        $this->assertTrue(can_transition('shipped', 'done'));
    }

    public function testCancelAllowedBeforeShipping(): void
    {
        $this->assertTrue(can_transition('new', 'canceled'));
        $this->assertTrue(can_transition('paid', 'canceled'));
    }

    public function testForbiddenTransitions(): void
    {
        $this->assertFalse(can_transition('new', 'paid'));        // нельзя перепрыгнуть оплату
        $this->assertFalse(can_transition('done', 'processing')); // терминальный статус
        $this->assertFalse(can_transition('canceled', 'paid'));   // терминальный статус
        $this->assertFalse(can_transition('shipped', 'canceled'));// отгружено — не отменить
    }
}
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `www/`):
```bash
php composer test
```
Expected: FAIL — `Error: Call to undefined function order_statuses()`.

- [ ] **Step 3: Реализовать функции**

`www/includes/order_status.php`:

```php
<?php
/** Все валидные статусы заказа. */
function order_statuses(): array
{
    return ['new', 'pending_payment', 'paid', 'processing', 'shipped', 'done', 'canceled'];
}

/** Разрешён ли переход между статусами. */
function can_transition(string $from, string $to): bool
{
    $allowed = [
        'new'             => ['pending_payment', 'canceled'],
        'pending_payment' => ['paid', 'canceled'],
        'paid'            => ['processing', 'canceled'],
        'processing'      => ['shipped', 'canceled'],
        'shipped'         => ['done'],
        'done'            => [],
        'canceled'        => [],
    ];
    return isset($allowed[$from]) && in_array($to, $allowed[$from], true);
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run (из `www/`):
```bash
php composer test
```
Expected: PASS (все тесты CartQuantity + OrderStatus зелёные).

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/order_status.php www/tests/OrderStatusTest.php
git commit -m "feat: статусная машина заказа с тестами"
```

---

## Task 4: Интерфейс PaymentGateway

**Files:**
- Create: `www/includes/payment/PaymentGateway.php`

**Interfaces:**
- Consumes: ничего.
- Produces: интерфейс `PaymentGateway` с методами:
  - `createPayment(array $order, string $returnUrl): array` — создаёт платёж, возвращает `['payment_id' => string, 'confirmation_url' => string]`.
  - `parseCallback(array $request, string $rawBody): array` — разбирает webhook, возвращает `['payment_id' => string, 'status' => string, 'paid' => bool]`.
  - `verifySignature(array $headers, string $rawBody): bool` — проверка подлинности webhook.

На этом этапе реализации провайдера НЕТ (она в Плане 4) — только контракт.

- [ ] **Step 1: Создать интерфейс**

`www/includes/payment/PaymentGateway.php`:

```php
<?php
/**
 * Контракт платёжного шлюза. Реализации: YooKassaGateway (План 4) и др.
 * Заказы не привязаны к конкретному провайдеру.
 */
interface PaymentGateway
{
    /**
     * @param array  $order     заказ из БД (id, order_number, total, items, customer ...)
     * @param string $returnUrl URL возврата покупателя после оплаты
     * @return array{payment_id:string, confirmation_url:string}
     */
    public function createPayment(array $order, string $returnUrl): array;

    /**
     * @return array{payment_id:string, status:string, paid:bool}
     */
    public function parseCallback(array $request, string $rawBody): array;

    public function verifySignature(array $headers, string $rawBody): bool;
}
```

- [ ] **Step 2: Проверить синтаксис**

Run:
```bash
cd "C:/Users/USER/Documents/Сайт/www" && php -l includes/payment/PaymentGateway.php
```
Expected: `No syntax errors detected in includes/payment/PaymentGateway.php`.

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/payment/PaymentGateway.php
git commit -m "feat: интерфейс PaymentGateway (абстракция платёжного шлюза)"
```

---

## Task 5: Конфиг-шаблон без секретов + фикс SITE_URL

**Files:**
- Create: `www/includes/config.example.php`
- Modify: `www/includes/config.php` (только строка `SITE_URL`; файл не коммитится)

**Interfaces:**
- Consumes: ничего.
- Produces: `config.example.php` — копия структуры `config.php` со всеми `define(...)`, но с плейсхолдерами вместо секретов. Реальный `config.php` остаётся локально с исправленным `SITE_URL`.

- [ ] **Step 1: Создать шаблон конфига (плейсхолдеры вместо секретов)**

`www/includes/config.example.php` — повторяет все константы реального конфига, но без значений-секретов:

```php
<?php
// ШАБЛОН конфигурации. Скопировать в config.php и заполнить реальными значениями.
// config.php НЕ коммитится (см. .gitignore).
require_once dirname(__FILE__) . '/functions.php';

define('RECAPTCHA_SITE_KEY', 'ВАШ_SITE_KEY');
define('RECAPTCHA_SECRET_KEY', 'ВАШ_SECRET_KEY');

date_default_timezone_set('Europe/Moscow');

define('ROOT_PATH', dirname(__DIR__));
define('INCLUDES_PATH', ROOT_PATH . '/includes');
define('ADMIN_PATH', ROOT_PATH . '/admin');
define('UPLOAD_PATH', ROOT_PATH . '/uploads');

define('AMOCRM_DOMAIN', 'ваш-аккаунт.amocrm.ru');
define('AMOCRM_ACCESS_TOKEN', 'ВАШ_ACCESS_TOKEN');
define('AMOCRM_CLIENT_ID', 'ВАШ_CLIENT_ID');
define('AMOCRM_CLIENT_SECRET', 'ВАШ_CLIENT_SECRET');
define('AMOCRM_REDIRECT_URI', 'https://zippaket-optom.ru/includes/amocrm.php');

define('DB_HOST', 'localhost');
define('DB_PORT', '3306');
define('DB_NAME', 'имя_базы');
define('DB_USER', 'пользователь_базы');
define('DB_PASS', 'ПАРОЛЬ_БАЗЫ');

define('SITE_NAME', 'ZIP-Пакеты оптом по всей России');
define('SITE_URL', 'https://zippaket-optom.ru/');
define('ADMIN_EMAIL', 'info@zip-site.ru');
define('SUPPORT_PHONE', '8 (800) 123-45-67');

define('SESSION_NAME', 'ZIP_SESSION');
define('SESSION_LIFETIME', 86400);
define('CSRF_TOKEN_NAME', 'csrf_token');

// Платёжный шлюз (ЮKassa) — заполнить на этапе Плана 4
define('YOOKASSA_SHOP_ID', 'ВАШ_SHOP_ID');
define('YOOKASSA_SECRET_KEY', 'ВАШ_СЕКРЕТНЫЙ_КЛЮЧ');

// Функция подключения к БД определяется в config.php (как сейчас).
```

> ПРИМЕЧАНИЕ исполнителю: сверить список `define(...)` с актуальным `config.php` и добавить недостающие константы с плейсхолдерами. Реальные значения НЕ переносить.

- [ ] **Step 2: Починить баг двойного https в реальном конфиге**

В `www/includes/config.php` заменить строку:

```php
define('SITE_URL', 'https://https://zippaket-optom.ru/');
```
на:
```php
define('SITE_URL', 'https://zippaket-optom.ru/');
```

- [ ] **Step 3: Проверить синтаксис обоих файлов**

Run:
```bash
cd "C:/Users/USER/Documents/Сайт/www" && php -l includes/config.example.php && php -l includes/config.php
```
Expected: `No syntax errors detected` для обоих.

- [ ] **Step 4: Убедиться, что config.php НЕ попадёт в коммит, а пример — попадёт**

Run:
```bash
cd "C:/Users/USER/Documents/Сайт"
git check-ignore www/includes/config.php   # должен вывести путь (игнорируется)
git status -s www/includes/                  # config.example.php — как новый файл
```
Expected: `config.php` игнорируется; `config.example.php` в untracked.

- [ ] **Step 5: Commit (только пример конфига)**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add www/includes/config.example.php
git commit -m "docs: добавить config.example.php (шаблон без секретов)"
```

---

## Task 6: Миграция БД — поля products, таблицы orders/order_items

**Files:**
- Create: `db/migrations/2026-06-18-orders-schema.sql`
- Create: `db/README.md`

**Interfaces:**
- Consumes: существующая таблица `products`.
- Produces: новые поля в `products`; таблицы `orders`, `order_items` (см. spec).

- [ ] **Step 1: Написать SQL-миграцию**

`db/migrations/2026-06-18-orders-schema.sql`:

```sql
-- Миграция: интернет-магазин (поля упаковки + заказы)
-- БД: c103264_zippaket_optom_ru, charset utf8mb4

-- 1. Поля упаковки у товаров
ALTER TABLE `products`
  ADD COLUMN `min_order_qty` INT(11) NOT NULL DEFAULT 1 AFTER `unit`,
  ADD COLUMN `qty_step` INT(11) NOT NULL DEFAULT 1 AFTER `min_order_qty`,
  ADD COLUMN `pack_label` VARCHAR(100) DEFAULT NULL AFTER `qty_step`;

-- 2. Заказы
CREATE TABLE `orders` (
  `id` INT(11) NOT NULL AUTO_INCREMENT,
  `order_number` VARCHAR(32) NOT NULL,
  `status` VARCHAR(20) NOT NULL DEFAULT 'new',
  `customer_type` ENUM('individual','company') NOT NULL DEFAULT 'individual',
  `customer_name` VARCHAR(255) NOT NULL,
  `phone` VARCHAR(32) NOT NULL,
  `email` VARCHAR(255) DEFAULT NULL,
  `company_name` VARCHAR(255) DEFAULT NULL,
  `inn` VARCHAR(12) DEFAULT NULL,
  `kpp` VARCHAR(9) DEFAULT NULL,
  `legal_address` VARCHAR(500) DEFAULT NULL,
  `needs_invoice` TINYINT(1) NOT NULL DEFAULT 0,
  `delivery_method` ENUM('pickup','courier','tk') NOT NULL DEFAULT 'pickup',
  `delivery_address` VARCHAR(500) DEFAULT NULL,
  `comment` TEXT DEFAULT NULL,
  `payment_method` ENUM('online','invoice') NOT NULL DEFAULT 'online',
  `items_total` DECIMAL(15,2) NOT NULL DEFAULT 0.00,
  `total` DECIMAL(15,2) NOT NULL DEFAULT 0.00,
  `payment_id` VARCHAR(64) DEFAULT NULL,
  `payment_status` VARCHAR(32) DEFAULT NULL,
  `created_at` TIMESTAMP NULL DEFAULT current_timestamp(),
  `updated_at` TIMESTAMP NULL DEFAULT current_timestamp() ON UPDATE current_timestamp(),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_order_number` (`order_number`),
  KEY `idx_status` (`status`),
  KEY `idx_payment_id` (`payment_id`),
  KEY `idx_created` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 3. Позиции заказа (snapshot цены/названия)
CREATE TABLE `order_items` (
  `id` INT(11) NOT NULL AUTO_INCREMENT,
  `order_id` INT(11) NOT NULL,
  `product_id` INT(11) DEFAULT NULL,
  `name_snapshot` VARCHAR(500) NOT NULL,
  `price_snapshot` DECIMAL(15,2) NOT NULL,
  `qty` INT(11) NOT NULL,
  `line_total` DECIMAL(15,2) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_order` (`order_id`),
  CONSTRAINT `fk_order_items_order` FOREIGN KEY (`order_id`)
    REFERENCES `orders` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- [ ] **Step 2: Документировать применение миграции**

`db/README.md`:

```markdown
# Миграции БД

Применять по порядку имён файлов в `migrations/`.

## Применение

Локально/на сервере (MySQL/MariaDB):

```bash
mysql -u <user> -p <db_name> < db/migrations/2026-06-18-orders-schema.sql
```

Или через phpMyAdmin: импорт SQL-файла.

## Откат (если потребуется)

```sql
DROP TABLE IF EXISTS `order_items`;
DROP TABLE IF EXISTS `orders`;
ALTER TABLE `products`
  DROP COLUMN `min_order_qty`,
  DROP COLUMN `qty_step`,
  DROP COLUMN `pack_label`;
```
```

- [ ] **Step 3: Проверить SQL на синтаксис локально (если доступна тестовая БД)**

Run (опционально, при наличии локальной MySQL):
```bash
mysql -u root -p --execute="CREATE DATABASE IF NOT EXISTS zip_test CHARACTER SET utf8mb4; USE zip_test;" 
# затем создать products-заглушку или применить дамп, после чего:
mysql -u root -p zip_test < db/migrations/2026-06-18-orders-schema.sql
```
Expected: миграция применяется без ошибок (таблицы созданы). Если локальной БД нет — пропустить, применить на dev-сервере.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/USER/Documents/Сайт"
git add db/migrations/2026-06-18-orders-schema.sql db/README.md
git commit -m "feat: миграция БД — поля упаковки products + таблицы orders/order_items"
```

---

## Task 7: Финальные гейты Плана 1

**Files:** —

**Interfaces:**
- Consumes: всё из Tasks 1–6.
- Produces: подтверждение готовности фундамента.

- [ ] **Step 1: Прогнать все тесты**

Run (из `www/`):
```bash
cd "C:/Users/USER/Documents/Сайт/www" && php composer test
```
Expected: PASS — все тесты CartQuantity + OrderStatus зелёные, 0 ошибок.

- [ ] **Step 2: Проверить синтаксис всех новых PHP-файлов**

Run:
```bash
cd "C:/Users/USER/Documents/Сайт/www"
php -l includes/cart_quantity.php && php -l includes/order_status.php && php -l includes/payment/PaymentGateway.php && php -l includes/config.example.php
```
Expected: `No syntax errors detected` для каждого.

- [ ] **Step 3: Убедиться, что секреты не попали в индекс**

Run:
```bash
cd "C:/Users/USER/Documents/Сайт"
git ls-files | grep -E "includes/config.php$|tg/config.php$" || echo "OK: секреты не отслеживаются"
```
Expected: `OK: секреты не отслеживаются`.

- [ ] **Step 4: Финальный коммит-чекпоинт (если есть незакоммиченное)**

```bash
cd "C:/Users/USER/Documents/Сайт"
git status
# при необходимости закоммитить остатки
```

---

## Self-Review (выполнено при написании плана)

- **Покрытие spec:** секреты → Task 5; баг SITE_URL → Task 5; поля упаковки products → Task 6; таблицы orders/order_items → Task 6; валидация мин/кратность → Task 2; статусы заказа → Task 3; абстракция PaymentGateway → Task 4; тест-харнес → Task 1. Реализация ЮKassa, каталог, корзина, checkout, уведомления, редизайн — в Планах 2–6 (вне объёма Плана 1).
- **Плейсхолдеры:** примечания исполнителю в Task 5 явно требуют сверки со списком констант — это инструкция, не пропуск. Код-шаги содержат полный код.
- **Согласованность типов:** `normalize_quantity`/`is_valid_quantity` (Task 2), `order_statuses`/`can_transition` (Task 3), методы `PaymentGateway` (Task 4) используются единообразно; названия полей БД совпадают со spec.
